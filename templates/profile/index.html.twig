{% extends 'base.html.twig' %}

{% block title %}Hello ProfileController!{% endblock %}

{% block body %}

    {% include '_partials/alerts.html.twig' %}

        <div class="content">
            {{ form_start(form) }}
                {{ form_row(form.email, { label: 'Adresse email:' }) }}

                {{ form_row(form.pseudo, { label: 'Pseudo:' }) }}

                {{ form_row (form.currentPassword, {label:'Mot de passe actuel',})}}

                {{ form_row(form.plainPassword.first, { label: 'Nouveau Mot de passe:' }) }}

                {{ form_row(form.plainPassword.second, { label: 'Confirmez le mot de passe:' }) }}

                <button type="submit" class="button is-fullwidth is-primary">Mettre à jour</button>
           
            {{ form_end(form) }}

        </div>
{% endblock %}
